// import All from '@api/all';
export default {
    // getErpChannel({ commit }, data) {
    //     return All.postAPI(data).then(res => {
    //         res.unshift({name: "全部", code: 0});
    //         commit("setErpChannel", res);
    //     });
    // }
};